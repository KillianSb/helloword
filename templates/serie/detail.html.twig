{% extends 'base.html.twig' %}

{% block title %}Hello SerieController!{% endblock %}

{% block body %}
    <div class="blockFilm">
        <h1>Detail de la serie : </h1>
        <h2>{{ serie.titre }}</h2>
        <img class="img-film" src="{{ asset('img/'~serie.image) }}" alt="{{ serie.titre }}">
    </div>
{% endblock %}